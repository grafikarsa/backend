# =============================================================================
# GRAFIKARSA BACKEND - UBUNTU SERVER PRODUCTION
# =============================================================================
# Copy file ini ke /opt/grafikarsa/backend/.env di server
# Ganti semua nilai dengan production values!
# =============================================================================

# Application
APP_ENV=production
APP_PORT=8080
APP_URL=https://api.grafikarsa.com

# Database (PostgreSQL)
# DB_HOST=postgres karena dalam Docker network
DB_HOST=postgres
DB_PORT=5432
DB_USER=grafikarsa
DB_PASSWORD=GANTI_PASSWORD_KUAT_32_CHAR
DB_NAME=grafikarsa
DB_SSLMODE=disable

# MinIO (Object Storage)
# MINIO_ENDPOINT=minio:9000 karena dalam Docker network
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=grafikarsa_minio_admin
MINIO_SECRET_KEY=GANTI_PASSWORD_KUAT_32_CHAR
MINIO_BUCKET=grafikarsa
MINIO_USE_SSL=false

# MinIO Presigned URL (untuk browser upload via Nginx)
# Ini adalah domain publik yang diakses browser
MINIO_PRESIGN_HOST=storage.grafikarsa.com
MINIO_PRESIGN_USE_SSL=true

# Public URL untuk akses file (via Nginx reverse proxy)
STORAGE_PUBLIC_URL=https://storage.grafikarsa.com/grafikarsa

# JWT Authentication
# Generate dengan: openssl rand -hex 32
JWT_ACCESS_SECRET=GANTI_DENGAN_64_CHAR_HEX
JWT_REFRESH_SECRET=GANTI_DENGAN_64_CHAR_HEX_BERBEDA
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=168h

# Admin Configuration
# Ganti dengan path rahasia untuk login admin
ADMIN_LOGIN_PATH=rahasia_admin_path

# CORS (sesuaikan dengan domain kamu)
CORS_ORIGINS=https://grafikarsa.com,https://www.grafikarsa.com

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_DURATION=1m
